name: botas

channels:
  - conda-forge
  - bioconda

dependencies:
  - python>=3.10
  - pip
  - pysam>=0.22
  - edlib>=1.3.9
  - biopython>=1.81

  # Development
  - cython>=3.0
  - pytest>=7.4
  - pytest-cov
  - black
  - ruff
  - mypy

  - pip:
      - build
      - twine
